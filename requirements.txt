# ============================================================
# requirements.txt — Student-Document-OCR (Placeholder)
# ============================================================
# This file is a placeholder for pip-compatible installs.
# Preferred: use pyproject.toml with `pip install -e .[dev]`
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Database ─────────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# ── Elasticsearch ────────────────────────────────────────────
elasticsearch[async]>=8.12.0

# ── Object Storage ─────────────────────────────────────────────────
supabase>=2.4.0            # Supabase Python SDK (Auth + Storage)
minio>=7.2.0               # Kept as optional fallback for local dev

# ── Task Queue ───────────────────────────────────────────────
celery[redis]>=5.4.0
redis>=5.0.0
flower>=2.0.0

# ── Image Processing ─────────────────────────────────────────
opencv-python-headless>=4.9.0
Pillow>=10.3.0
pdf2image>=1.17.0
pytesseract>=0.3.10

# ── OCR (uncomment as needed) ────────────────────────────────
# vietocr>=0.3.0          # TODO: Install after model setup
# paddlepaddle>=2.6.0     # TODO: Install GPU version separately
# paddleocr>=2.7.0        # TODO: Install after PaddlePaddle

# ── Authentication ─────────────────────────────────────────────────
# python-jose kept for decoding Supabase JWT tokens in FastAPI middleware
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4     # Kept for password hash verify if needed
python-multipart>=0.0.9

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0
loguru>=0.7.0
tenacity>=8.3.0
aiofiles>=23.2.0

# ── Dev / Test ───────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.4.0
mypy>=1.10.0
pre-commit>=3.7.0

# ── AI / ML (Training — install separately in GPU env) ───────
# torch>=2.3.0            # TODO: See training/README.md
# torchvision>=0.18.0
# transformers>=4.41.0
# datasets>=2.19.0
# albumentations>=1.4.0
# tensorboard>=2.17.0
